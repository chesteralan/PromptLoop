import { describe, it } from 'vitest'

describe('shared types', () => {
  it('type-check all definitions at compile time', () => {
    // This file validates that the types module compiles without errors.
    // No runtime assertions needed — tsc/vitest compilation is the test.
  })
})
